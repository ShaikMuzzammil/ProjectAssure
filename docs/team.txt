Team NEXGEN — ProjectAssure (SIH 2026 · SIH26103 · MoSPI)
Amrita Vishwa Vidyapeetham, Chennai Campus
════════════════════════════════════════════════════════

This roster is mirrored in:
  - prototype/src/lib/projectassure/team.ts (read by the About page)
  - host-control/src/lib/host/seed.ts (read by the host-control UI)
Keep both files in sync if you change names or roles.

1. Harshavardhan (HV) — Team Lead · Platform Architecture
   Owns the overall architecture, the secure sign-in system, the
   deployment pipeline and the host-control integration layer.

2. Shaik Muzzammil (SM) — Intelligence Assistant · Multi-Provider AI Chain
   Built the Gemini-first provider chain, the ReAct agent loop,
   document grounding and the universal-mode intelligence centre.

3. Kalathuru Varshitha (KV) — Prediction Engine · ML & Risk Modelling
   Designed the 18-signal risk engine, the delay/cost prediction
   models with explainable factors and the health-score composite.

4. Keerthana Varapradha NB (KP) — Document Intelligence · OCR & RAG
   Built the in-browser document reader, the 45-pattern risk scanner,
   the vector index and the citation pipeline.

5. Nishitha Penagaluru (NP) — UI/UX · Design System & Workflows
   Designed the seven-feature cockpit, the guided workflows, the
   light/dark themes and the responsive design system.

6. A. Gandhimathi (GM) — Quality · Testing, Reports & Documentation
   Runs end-to-end verification, owns the PDF/Excel/CSV export
   pipeline, the email delivery centre and the audit trail.
