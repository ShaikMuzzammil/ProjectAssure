"use client";
// v21: This view is replaced by security-audit.tsx (combined audit + access + webhooks).
// Kept as a re-export so any external link resolves to the new component.
export { SecurityAudit as AuditTrail } from "./security-audit";
